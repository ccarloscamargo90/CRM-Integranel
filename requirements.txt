# =====================================================================
# CRM-Granos-MX — Dependencias de producción
# =====================================================================
# Instalar con:  pip install -r requirements.txt
# Para dev (linting, tests, notebooks) usar pyproject.toml:
#                pip install -e ".[dev]"
# =====================================================================

# Core
python-dotenv==1.0.1
pydantic==2.10.6
pydantic-settings==2.7.1
loguru==0.7.3

# Base de datos (PostgreSQL + PostGIS)
sqlalchemy==2.0.36
psycopg[binary]==3.2.4
alembic==1.14.0
geoalchemy2==0.16.0

# HTTP / retries
httpx==0.28.1
tenacity==9.0.0

# Manipulación de datos
pandas==2.2.3
numpy==2.2.1
rapidfuzz==3.10.1

# Geo
shapely==2.0.6
geopandas==1.0.1
geopy==2.4.1

# API REST + auth
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-jose[cryptography]==3.3.0
bcrypt==5.0.0
python-multipart==0.0.20

# Scheduling
apscheduler==3.11.0

# Templating server-side (Dashboard server-rendered Jinja+HTMX)
jinja2==3.1.5
