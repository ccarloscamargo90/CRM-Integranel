"""core — configuración, conexión a DB, logging y modelos SQLAlchemy.

Módulos planeados (se implementan en Fase 1+):
- config.py     → Pydantic Settings que lee .env
- db.py         → engine SQLAlchemy + sesión + dependencia FastAPI
- logging.py    → loguru con sinks por nivel y archivo
- models.py     → modelos ORM declarativos (cuando los necesitemos)
"""
